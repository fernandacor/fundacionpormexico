const MyUrlField = require('./MyUrlField');
const record = require('./MyUrlField');

describe('MyUrlField', () => {
    test('MyUrlField', () => {
        expect(MyUrlField).toBeDefined();
    });
    test ('record', () => {
        expect(record).toBeTruthy();
    });
});